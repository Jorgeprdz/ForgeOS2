# MOD-SCAFFOLD-CATALOG

Status: `WAVE 6 REVIEW PACKAGE / NOT INSTALLED`

## Responsibility

Bind the four current canonical documentary scaffold families to:

- exact active authority snapshot;
- exact versioned definition;
- deterministic runtime template;
- exact input schema;
- example input;
- exact output family;
- active or reference-only instantiation status.

## Canonical families

1. Architecture Boundary — SG-002 authorized.
2. Dependency and Relationship — SG-001 reference-only; SG-002 blocks standalone instances.
3. Domain Responsibility — SG-002 authorized.
4. Source of Truth — SG-002 authorized only for already-established owners.

## Source preservation

The runtime templates are derived renderable assets. They do not replace or mutate the current canonical Markdown source documents under `docs/architecture/scaffolds/`.
