#!/usr/bin/env bash
set -Eeuo pipefail

SCRIPT_PATH="$(realpath -- "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(dirname -- "$SCRIPT_PATH")"
ROOT="$(dirname -- "$SCRIPT_DIR")"

exec node \
  "$ROOT/forge/autopilot/global-autopilot.mjs" \
  "$@"
