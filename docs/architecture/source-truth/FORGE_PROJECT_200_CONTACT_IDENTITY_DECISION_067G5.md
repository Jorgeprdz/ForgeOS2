# Forge Project 200 Contact Identity Decision 067G5

Status: `CONTRACT_IMPLEMENTED`. Stable identity is a deterministic digest of Project ID, immutable Project version and exact source-row reference. Project 200 Contact ID is not Prospect ID and creates no prospect automatically. NASAT/private evaluation fields are rejected; minimum contact/relationship/consent/evidence fields are allowlisted. Duplicate source rows are surfaced, never silently overwritten. Production persistence and handoff remain blocked until their modules. Next: 067G6.
