# 107Z15E7R Ephemeral Store Canonical Round-Trip Repair

Status: PASS

## Decision

The Quote Preview canonical persistence path is proven with the existing in-memory backend and requires no source change.

## Proven flow

`controlled text fixture → real extraction engine → canonical persistence builder → official record contract → memory store write/read → fresh memory backend absence`

## Authorized APIs

- `buildQuotePreviewPdfCanonicalPersistenceInput({ nativeResult, context })`
- `createMemoryBackend()`
- `createStore({ backend, now })`
- `writePreviewResult(recordInput)`
- `readPreviewResult(identity)`
- `createRecord(recordInput)`
- `validateRecord(record)`

## Invariants

- Exactly eight canonical fields are persisted.
- ALFA and BETA are generated by changing the real `Asegurado:` fixture line.
- The write result identity is used for reads.
- A fresh backend reports absence through `PREVIEW_RESULT_NOT_FOUND`.
- No localStorage, browser, PDF read, external backend, or quote-truth promotion occurs.
- Source change required: false.

## Cleanup strategy

`DISCARD_MEMORY_BACKEND_INSTANCE_AND_PROVE_FRESH_INSTANCE_NOT_FOUND`

## Next gate

`107Z15E8_CONTROLLED_BROWSER_CANONICAL_PERSISTENCE_INTEGRATION_DECISION_GATE`
