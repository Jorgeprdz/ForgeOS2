"use strict";
(function(global){
const esc=value=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]));
const SOURCES=["Referido","Precontrato","Proyecto 200","Mercado natural","Redes sociales","Cliente existente","Evento","Otro"];
const LABELS={referred_new:"Referido nuevo",contacted:"Contactado",appointment_scheduled:"Cita agendada",needs_analysis:"Análisis",proposal_presented:"Propuesta",decision_pending:"Decisión",won:"Ganado",lost:"Perdido"};
const field=(name,label,type="text",extra="")=>`<label>${esc(label)}<input name="${esc(name)}" type="${type}" ${extra}></label>`;
function formTemplate(prospect={}){const value=(name)=>esc(prospect[name]??"");return `<dialog class="forge-prospect-dialog" data-prospect-form-dialog aria-labelledby="prospect-form-title"><form method="dialog" data-prospect-form novalidate><header><div><p class="forge-pipeline-product">PROSPECTO PRODUCTIVO</p><h2 id="prospect-form-title">${prospect.id?"Editar prospecto":"Agregar prospecto"}</h2></div><button type="button" data-close-prospect-form aria-label="Cerrar">×</button></header><div class="forge-prospect-form-grid">${field("fullName","Nombre completo *","text",`required value="${value("fullName")}"`)}${field("phone","Teléfono","tel",`value="${value("phone")}"`)}${field("whatsapp","WhatsApp","tel",`value="${value("whatsapp")}"`)}${field("email","Correo","email",`value="${value("email")}"`)}<label>Fuente *<select name="source" required><option value="">Selecciona una fuente</option>${SOURCES.map(source=>`<option ${prospect.source===source?"selected":""}>${esc(source)}</option>`).join("")}</select></label><div class="forge-prospect-referral" data-referral-fields>${field("referrerName","Referido por","text",`value="${value("referrerName")}"`)}${field("referrerRelationship","Relación con el referente","text",`value="${value("referrerRelationship")}"`)}</div>${field("dateOfBirth","Fecha de nacimiento","date",`value="${value("dateOfBirth")}"`)}${field("age","Edad","number",`min="0" max="130" value="${value("age")}"`)}${field("maritalStatus","Estado civil","text",`value="${value("maritalStatus")}"`)}${field("dependents","Dependientes","number",`min="0" value="${value("dependents")}"`)}${field("occupation","Ocupación","text",`value="${value("occupation")}"`)}${field("estimatedIncome","Ingreso estimado","number",`min="0" step="0.01" value="${value("estimatedIncome")}"`)}${field("productsOfInterest","Productos de interés","text",`value="${value("productsOfInterest")}"`)}<label class="forge-prospect-wide">Contexto inicial *<textarea name="initialContext" required>${value("initialContext")}</textarea></label>${field("nextActionType","Próxima acción","text",`value="${value("nextActionType")}"`)}${field("nextActionAt","Fecha de próxima acción","datetime-local",`value="${value("nextActionAt")}"`)}</div><p class="forge-prospect-form-error" data-prospect-form-error role="alert" hidden></p><footer><button type="button" data-close-prospect-form>Cancelar</button><button type="submit" class="forge-pipeline-primary" data-save-prospect>${prospect.id?"Guardar cambios":"Guardar prospecto"}</button></footer></form></dialog>`;}
function detailTemplate(prospect){const row=(label,value)=>value!==null&&value!==undefined&&value!==""?`<div><dt>${esc(label)}</dt><dd>${esc(value)}</dd></div>`:"";const phone=prospect.phone||prospect.phoneNormalized||prospect.whatsapp||prospect.whatsappNormalized||"";const whatsapp=prospect.whatsapp||prospect.whatsappNormalized||prospect.phone||prospect.phoneNormalized||"";return `<dialog class="forge-prospect-dialog forge-prospect-detail-dialog" data-prospect-detail-dialog aria-labelledby="prospect-detail-title"><article><header><div><p class="forge-pipeline-product">${esc(LABELS[prospect.status]||prospect.status)}</p><h2 id="prospect-detail-title">${esc(prospect.fullName)}</h2></div><button type="button" data-close-prospect-detail aria-label="Cerrar">×</button></header><dl class="forge-prospect-detail-list">${row("Teléfono",phone)}${row("WhatsApp",whatsapp)}${row("Correo",prospect.email)}${row("Fuente",prospect.source)}${row("Referido por",prospect.referrerName)}${row("Relación",prospect.referrerRelationship)}${row("Fecha de nacimiento",prospect.dateOfBirth)}${row("Edad",prospect.age)}${row("Estado civil",prospect.maritalStatus)}${row("Dependientes",prospect.dependents)}${row("Ocupación",prospect.occupation)}${row("Ingreso estimado",prospect.estimatedIncome)}${row("Productos de interés",Array.isArray(prospect.productsOfInterest)?prospect.productsOfInterest.join(", "):prospect.productsOfInterest)}${row("Contexto inicial",prospect.initialContext)}${row("Próxima acción",prospect.nextActionType)}${row("Fecha de seguimiento",prospect.nextActionAt)}${row("Fecha de creación",prospect.createdAt)}</dl><section class="forge-prospect-contact"><label>Tono de WhatsApp<select data-whatsapp-tone><option value="cercano">Cercano</option><option value="profesional">Profesional</option><option value="ejecutivo">Ejecutivo</option></select></label></section><footer><button type="button" data-edit-prospect>Editar</button><button type="button" data-archive-prospect>Eliminar</button><a class="forge-pipeline-action ${phone?"":"is-disabled"}" ${phone?`href="tel:${esc(phone)}"`:"aria-disabled=\"true\" title=\"No hay un número válido\""}>Llamar</a><a class="forge-pipeline-action ${whatsapp?"":"is-disabled"}" data-whatsapp-action ${whatsapp?`href="${esc(whatsappUrl(prospect,"profesional"))}" target="_blank" rel="noopener noreferrer"`:"aria-disabled=\"true\" title=\"No hay un número válido\""}>WhatsApp</a></footer></article></dialog>`;}
function whatsappUrl(prospect,tone){const number=String(prospect.whatsappNormalized||prospect.phoneNormalized||prospect.whatsapp||prospect.phone||"").replace(/\D/g,"");const introductions={cercano:"Qué gusto saludarte",profesional:"Me gustaría conversar contigo",ejecutivo:"Quisiera coordinar una conversación"};const context=prospect.initialContext||prospect.source||"la referencia que recibí";const text=`Hola, ${prospect.fullName}. Soy tu asesor.\n\n${introductions[tone]||introductions.profesional} porque ${context}.\n\nMe gustaría conocer tus objetivos y revisar cómo podría ayudarte.`;return `https://wa.me/${number}?text=${encodeURIComponent(text)}`;}
function toModel(prospects){const groups=new Map();for(const prospect of prospects){const status=prospect.status||"referred_new";if(!groups.has(status))groups.set(status,[]);groups.get(status).push({prospectId:prospect.id,name:prospect.fullName,sourceLabel:prospect.source||"Fuente verificada",stageLabel:LABELS[status]||status,lastVerifiedActivity:prospect.updatedAt||prospect.createdAt,nextCommitment:prospect.nextActionType||"Sin compromiso"});}return {state:prospects.length?"ready":"empty",message:"Todavía no tienes prospectos. Agrega el primero para comenzar tu Pipeline.",writerAvailable:true,columns:[...groups].map(([columnId,items])=>({columnId,label:LABELS[columnId]||columnId,items}))};}
function values(form){const data=Object.fromEntries(new FormData(form));for(const key of ["age","dependents","estimatedIncome"])if(data[key]!=="")data[key]=Number(data[key]);else delete data[key];for(const [key,value] of Object.entries(data))if(value==="")delete data[key];return data;}
function create({client,root,renderPipeline=global.ForgePipelineUI?.renderPipelineUI}){if(!client||!root||typeof renderPipeline!=="function")throw new Error("PRODUCTIVE_PIPELINE_DEPENDENCY_MISSING");const service=global.ForgeProductiveProspectService067G17B.create(client);let prospects=[];let selected=null;let submitting=false;
function render(model=toModel(prospects)){root.innerHTML=renderPipeline(model);root.querySelector(".forge-pipeline")?.setAttribute("data-productive-prospect-pipeline","067g17b");}
async function load(){render({state:"loading",message:"Cargando tus prospectos…",writerAvailable:true});try{prospects=await service.listProspects();render();}catch(error){render({state:"error",message:error.code==="AUTH_REQUIRED"?"Tu sesión expiró. Inicia sesión nuevamente.":"No pudimos cargar tus prospectos. Intenta nuevamente.",writerAvailable:false});}}
function openForm(prospect={}){root.insertAdjacentHTML("beforeend",formTemplate(prospect));const dialog=root.querySelector("[data-prospect-form-dialog]");dialog.dataset.prospectId=prospect.id||"";const source=dialog.querySelector('[name="source"]');const syncReferral=()=>{dialog.querySelector("[data-referral-fields]").hidden=source.value!=="Referido";};source.addEventListener("change",syncReferral);syncReferral();dialog.showModal();dialog.querySelector('[name="fullName"]').focus();}
function closeForm(){const dialog=root.querySelector("[data-prospect-form-dialog]");if(!dialog)return;if(new FormData(dialog.querySelector("form")).entries().next().done===false&&dialog.querySelector("form").dataset.dirty==="true"&&!global.confirm("Hay cambios sin guardar. ¿Quieres cerrar?"))return;dialog.close();dialog.remove();}
function openDetail(prospect){selected=prospect;root.querySelector("[data-prospect-detail-dialog]")?.remove();root.insertAdjacentHTML("beforeend",detailTemplate(prospect));root.querySelector("[data-prospect-detail-dialog]").showModal();}
async function submit(form){if(submitting)return;const errorNode=form.querySelector("[data-prospect-form-error]");errorNode.hidden=true;if(!form.reportValidity())return;const input=values(form);if(!input.phone&&!input.whatsapp){errorNode.textContent="Ingresa teléfono o WhatsApp.";errorNode.hidden=false;return;}submitting=true;const save=form.querySelector("[data-save-prospect]");save.disabled=true;save.textContent="Guardando…";try{const id=form.closest("dialog").dataset.prospectId;const prospect=id?await service.updateProspect(id,input):await service.createProspect(input);prospects=id?prospects.map(item=>item.id===id?prospect:item):[prospect,...prospects];form.closest("dialog").close();form.closest("dialog").remove();render();openDetail(prospect);}catch(error){errorNode.textContent=error.code==="DUPLICATE_PROSPECT"?"Este prospecto ya existe en tu Pipeline.":error.code==="AUTH_REQUIRED"?"Tu sesión expiró. Inicia sesión nuevamente.":error.code==="VALIDATION_ERROR"?"Revisa los campos marcados.":"No pudimos guardar el prospecto.";errorNode.hidden=false;}finally{submitting=false;if(save.isConnected){save.disabled=false;save.textContent=selected?"Guardar cambios":"Guardar prospecto";}}}
async function archive(){if(!selected||!global.confirm("¿Quieres retirar este prospecto del Pipeline?\n\nSu historial se conservará y no se eliminará físicamente."))return;await service.archiveProspect(selected.id);prospects=prospects.filter(item=>item.id!==selected.id);root.querySelector("[data-prospect-detail-dialog]")?.remove();selected=null;render();}
root.addEventListener("input",event=>{event.target.closest("[data-prospect-form]")?.setAttribute("data-dirty","true");});root.addEventListener("submit",event=>{if(!event.target.matches("[data-prospect-form]"))return;event.preventDefault();void submit(event.target);});root.addEventListener("click",event=>{const add=event.target.closest("[data-add-prospect]");const open=event.target.closest("[data-open-prospect]");if(add){openForm();return;}if(open){const prospect=prospects.find(item=>item.id===open.dataset.openProspect);if(prospect)openDetail(prospect);return;}if(event.target.closest("[data-close-prospect-form]")){closeForm();return;}if(event.target.closest("[data-close-prospect-detail]")){event.target.closest("dialog").close();event.target.closest("dialog").remove();return;}if(event.target.closest("[data-edit-prospect]")){root.querySelector("[data-prospect-detail-dialog]")?.close();root.querySelector("[data-prospect-detail-dialog]")?.remove();openForm(selected);return;}if(event.target.closest("[data-archive-prospect]")){void archive();}});root.addEventListener("change",event=>{if(!event.target.matches("[data-whatsapp-tone]"))return;const link=root.querySelector("[data-whatsapp-action]");if(link&&selected)link.href=whatsappUrl(selected,event.target.value);});return Object.freeze({load,openForm,openDetail,diagnostics:()=>({prospectCount:prospects.length,selectedProspectId:selected?.id||null,submitting})});}
const api=Object.freeze({create,formTemplate,detailTemplate,toModel,whatsappUrl});global.ForgeProductiveProspectUI067G17B=api;if(typeof module!=="undefined"&&module.exports)module.exports=api;
})(typeof globalThis!=="undefined"?globalThis:window);
