# SG-023 - Ownership And Source-Of-Truth Registry

Canonical order: 6.

Layer: `OWNERSHIP_AND_SOURCE_OF_TRUTH_REGISTRY`.

Status: `BLOCKED_REQUIRES_OWNER_DECISION`.

Depends on: `SG-021`, `SG-022`.

Produces: `source_of_truth_registry`, `metric_claim_owner_registry`, `forbidden_parallel_truth_policy`.

This stage is part of the dependency-ordered Forge OS 2 rewrite sequence. It must be executed through Bash-based Termux tooling and must not be selected by numeric SG order.
