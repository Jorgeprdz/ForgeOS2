# SG-027 - Read Model And Context Envelope Contracts

Canonical order: 10.

Layer: `READ_MODEL_AND_CONTEXT_ENVELOPE_CONTRACTS`.

Status: `BLOCKED_REQUIRES_OWNER_DECISION`.

Depends on: `SG-022`, `SG-023`, `SG-026`.

Produces: `read_model_contract`, `context_envelope_contract`, `purpose_limitation_policy`, `forbidden_inference_contract`.

This stage is part of the dependency-ordered Forge OS 2 rewrite sequence. It must be executed through Bash-based Termux tooling and must not be selected by numeric SG order.
