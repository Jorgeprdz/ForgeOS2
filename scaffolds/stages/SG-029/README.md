# SG-029 - Manager Workspace Surface Contract

Canonical order: 25.

Layer: `WORKSPACE_SURFACE`.

Status: `undefined`.

Depends on: `SG-013`, `SG-024`, `SG-027`.

Produces: `manager_workspace_surface_contract`.

This stage is part of the dependency-ordered Forge OS 2 rewrite sequence. It must be executed through Bash-based Termux tooling and must not be selected by numeric SG order.
