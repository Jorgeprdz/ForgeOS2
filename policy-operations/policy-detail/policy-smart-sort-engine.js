/*
|--------------------------------------------------------------------------
| MODULE: policy-smart-sort-engine.js
|--------------------------------------------------------------------------
|
| Smart sorting cartera.
|
|--------------------------------------------------------------------------
*/

export function ordenarPolizas({

    polizas = [],

    sortBy = 'prima'

}) {

    switch (sortBy) {

        /*
        |--------------------------------------------------------------------------
        | Prima
        |--------------------------------------------------------------------------
        */

        case 'prima':

            return [

                ...polizas

            ].sort(

                (a, b) =>

                    b.prima
                    - a.prima
            );

        /*
        |--------------------------------------------------------------------------
        | Renovación
        |--------------------------------------------------------------------------
        */

        case 'renovacion':

            return [

                ...polizas

            ].sort(

                (a, b) =>

                    new Date(
                        a.fechaRenovacion
                    )

                    -

                    new Date(
                        b.fechaRenovacion
                    )
            );

        default:

            return polizas;
    }
}